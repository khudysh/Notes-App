import { createStore } from 'vuex'
import noteState from './noteState'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    noteState: noteState,
  }
})
